<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.courseflow</groupId>
        <artifactId>courseflow-parent</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <artifactId>courseflow-app</artifactId>
    <packaging>jar</packaging>

    <!--
    Module com.courseflow.app : interface graphique JavaFX, dépend de com.courseflow.core pour
    le modèle/la persistance. La commande "lecture" (CLI) vit dans "core" et n'a jamais besoin de
    résoudre ce module.
    -->

    <properties>
        <javafx.mainClass>com.courseflow.app/com.courseflow.app.App</javafx.mainClass>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.courseflow</groupId>
            <artifactId>courseflow-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-fxml</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.mkpaz</groupId>
            <artifactId>atlantafx-base</artifactId>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <!-- Pilote les panes JavaFX comme un utilisateur (clics, saisie clavier) pour tester
                 les parcours critiques de l'UI, seul module non couvert jusqu'ici (voir doc/BILAN_ET_PISTES.md §8). -->
            <groupId>org.testfx</groupId>
            <artifactId>testfx-junit5</artifactId>
            <version>4.0.18</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-maven-plugin</artifactId>
                <version>0.0.8</version>
                <configuration>
                    <mainClass>${javafx.mainClass}</mainClass>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!-- TestFX pilote l'Application JavaFX par réflexion pour l'initialiser/la
                         nettoyer entre les tests ; sans cette ouverture, le nettoyage échoue avec
                         une IllegalAccessException bruyante (mais sans faire échouer les tests). -->
                    <argLine>--add-opens javafx.graphics/com.sun.javafx.application=ALL-UNNAMED</argLine>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
