package com.courseflow.ui;

/**
 * Couleurs de l'interface qui ne viennent pas des jetons du thème (voir les fichiers
 * {@code charte-*.css} pour ceux-là).
 */
final class Couleurs {

    /**
     * Couleur d'affichage d'un cours qui n'a pas de couleur choisie par l'utilisateur (gris
     * neutre). Utilisée partout où un cours est rendu : Accueil, grille de l'emploi du temps,
     * pastilles de la liste des cours.
     */
    static final String COURS_SANS_COULEUR = "#95a5a6";

    private Couleurs() {
    }
}
